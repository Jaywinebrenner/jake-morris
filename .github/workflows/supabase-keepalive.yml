name: Supabase Keepalive

on:
  schedule:
    - cron: "0 12 * * *"   # once per day
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl -s https://itfuxxajrvqatumlnogg.supabase.co/rest/v1/ > /dev/null